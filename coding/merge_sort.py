import unittest
from collections.abc import Iterable

class Sorting:

	def __init__(self, values: Iterable):
		self.values = values

	def merge_sort(self):

		len = len(self.values)

	def merge(self, )


	def __repr__(self):
		return str(self.values)